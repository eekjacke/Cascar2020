def lattice_planner(mission, heuristic, f_next):

    while not q.IsEmpty():
        prio, x = q.pop()
        if x == goalNode
            foundPlan = true
            break

        nextState, motionPrimitive, distance = f_next(x)
        for xi, ui, di in zip(nextState, motionPrimitive, distance):
            if previous[xi]

    return
